class UmpleToJava {
    association_GetOne_clear <<!<</*association_GetOne_clear*/>>
  <<#if (model.isDebugMode()) {
    Method g_method = new Method("protected", "clear_" + gen.translate("associationOne",av), "void", false);
    debugAutogeneratedMethod(g_method, uClass, model);
  }#>>
  protected void clear_<<=gen.translate("associationOne",av)>>()
  {
    <<=gen.translate("associationOne",av)>> = null;
  }
!>>
}
