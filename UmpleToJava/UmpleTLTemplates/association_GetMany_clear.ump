class UmpleToJava {
    association_GetMany_clear <<!<</*association_GetMany_clear*/>>
  <<#if (model.isDebugMode()) {
    Method g_method = new Method("protected", "clear_" + gen.translate("associationMany",av), "void", false);
    debugAutogeneratedMethod(g_method, uClass, model);
  }#>>
  protected void clear_<<=gen.translate("associationMany",av)>>()
  {
    <<=gen.translate("associationMany",av)>>.clear();
  }
!>>
}
